<style is:inline>
	:root {
		--color-text-heading: var(--c-ink);
		--color-text-default: var(--c-ink);
		--color-text-muted: var(--c-ink-2);

		--color-bg-page: var(--c-ground);
		--color-bg-page-dark: var(--c-sunk);
		--color-bg-header: var(--c-surface);

		--c-link-hover: var(--c-accent-700);

		--swiper-navigation-color: var(--color-accent);
		--swiper-pagination-color: var(--color-accent);
		--swiper-pagination-bullet-inactive-color: var(--color-gray-800);

		--scroll-progress-bg: var(--c-line);
		--scroll-progress-bar-bg: var(--c-accent);
		--content-width: var(--breakpoint-xl);

		--scroll-progress-height: 0.25rem;

		--header-height: 4rem;

		--sidebar-width: 344px;
		--sidebar-width-small: 250px;

		::selection {
			background-color: lavender;
		}
	}
	.dark {
		--color-text-heading: var(--c-ink);
		--color-text-default: var(--c-ink);
		--color-text-muted: var(--c-ink-2);

		--color-bg-page: var(--c-ground);
		--color-bg-page-dark: var(--c-sunk);
		--color-bg-header: var(--c-surface);

		--c-link-hover: var(--c-accent-400);

		--scroll-progress-bg: var(--c-line);
		--scroll-progress-bar-bg: var(--c-accent);

		::selection {
			background-color: black;
			color: snow;
		}
	}
</style>
