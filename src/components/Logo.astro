---
import { Image } from "astro:assets";

export interface Props {
	class?: string;
}

const { class: className = "" } = Astro.props;
---

<div class="flex items-center gap-3 min-h-[45px] whitespace-nowrap self-center" class:list={[className]}>
	<span class="font-bold font-primary text-xl md:text-2xl"
		>Frank
		<span class="font-normal ml-2">Fava</span></span
	>
</div>
