---
/**
 * Practicalities page.
 */

import { blueprints } from "@/data/blueprints";
import SubIndexedLayout from "@/layouts/SubIndexedLayout.astro";

const entry = blueprints.practicalities;

const metadata = {
	title: entry.title,
	description: entry.description,
};
---

<SubIndexedLayout {...{ metadata, blueprint: entry }}>
	<Fragment slot="lead">Rights, language, notice and location. The screening questions, answered before you have to ask them.</Fragment>
</SubIndexedLayout>
