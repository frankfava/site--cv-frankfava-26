---
import PageLayout from "@/layouts/PageLayout.astro";

import CONFIG from "site:config";

const metadata = {};
---

<PageLayout metadata={metadata}>
	<pre class="p-5 text-sm whitespace-pre-wrap">{JSON.stringify(CONFIG, null, 4)}</pre>
</PageLayout>
